% fig:adni-cycle-b2a: both cycle directions in one figure, (a) T1->FA->T1 and
% (b) FA->T1->FA, two held-out slices each -- the slices of fig:adni-path-ours. DEC-FA; one shared
% error scale.
% Rendered by inpaper_utils/cycle_ab.sbatch (make_flow_viz --figures cycle_ab --ab-pick).
\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/adni_cycle.pdf}
\caption{Translating across two modalities on held-out testing slices. The \emph{latent cycle} inverts the flow output directly, the \emph{image cycle} decodes the translation to pixels and re-encodes it. (a) for T1$\rightarrow$DWI-FA and back. (b) The same cycles in the DWI-FA$\rightarrow$T1 direction.}
\label{fig:adni-cycle-b2a}
\end{figure}
