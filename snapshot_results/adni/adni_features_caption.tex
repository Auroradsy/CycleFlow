% fig:adni-flow-features: the symmetric feature-map figure for morph-smooth, included at
% 0.92\linewidth so the caption fits on the same float page. Numbers quoted "over the test
% set" are means over the 430 held-out ADNI slices.
% Rendered by make_flow_viz --figures features_sym.
\begin{figure}[t]
\centering
\includegraphics[width=0.8\linewidth]{figures/adni_features.pdf}
\caption{Feature maps along the flow, T1$\rightarrow$DWI-FA (\emph{morph}), for held-out subjects, each subject occupies two rows. \emph{PCA}: the $256\times28\times28$ state after every block $f_k$, projected onto three principal directions and shown as RGB. \emph{$\|\Delta\|$}: the per-location RMS change from the previous column, which sees all $256$ channels}
\label{fig:adni-flow-features}
\end{figure}
