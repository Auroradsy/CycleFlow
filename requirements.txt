# Versions this was developed and run against (CUDA 11.3, python 3.8).
# Nothing here is version-critical except that torch must have CUDA.
torch==1.12.0
numpy>=1.21
nibabel>=3.2
scikit-image>=0.19
matplotlib>=3.5
